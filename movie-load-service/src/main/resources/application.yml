
#H2 data base configuration
#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    generate-ddl: true
#    hibernate:
#      ddl-auto: update
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

#server properties for AWS elastic bean stack
server:
  port: 8080


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rate_movies?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: ratemovies123
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

  application:
    name: movieloadservice
  profiles:
    active: default


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/








